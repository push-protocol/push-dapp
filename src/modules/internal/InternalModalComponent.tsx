import { ModalInnerComponentType } from 'hooks/useModalBlur';

export const InternalModalContent = ({ onClose, onConfirm, InnerComponentProps }: ModalInnerComponentType) => {
  return <h1>Hello</h1>;
};
