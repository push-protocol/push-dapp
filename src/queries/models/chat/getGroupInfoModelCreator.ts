import { GroupInfoResponse } from '../../types';

//any remodelling needed in the response can be done here
export const getGroupInfoModelCreator = (response: GroupInfoResponse): GroupInfoResponse => response;
