import { RemoveChannelDelegateResponse } from '../../types';

//any remodelling needed in the response can be done here
export const removeChannelDelegateModelCreator = (
  response: RemoveChannelDelegateResponse
): RemoveChannelDelegateResponse => response;
