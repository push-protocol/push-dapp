import { AliasInfoResponse } from '../../types';

//any remodelling needed in the response can be done here
export const getAliasInfoModelCreator = (response: AliasInfoResponse): AliasInfoResponse => response;
