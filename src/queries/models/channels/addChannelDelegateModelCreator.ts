import { AddChannelDelegateResponse } from '../../types';

//any remodelling needed in the response can be done here
export const addChannelDelegateModelCreator = (response: AddChannelDelegateResponse): AddChannelDelegateResponse =>
  response;
