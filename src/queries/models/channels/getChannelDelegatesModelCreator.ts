import { ChannelDelegatesResponse } from '../../types';

//any remodelling needed in the response can be done here
export const getChannelDelegatesModelCreator = (response: ChannelDelegatesResponse): ChannelDelegatesResponse =>
  response;
