import { SendNotificationParams } from '../../types';

//any remodelling needed in the response can be done here
export const sendNotificationModelCreator = (response: SendNotificationParams): SendNotificationParams => response;
