import { UserProfileDetailsResponse } from '../../types';

//any remodelling needed in the response can be done here
export const getUserProfileDetailsModelCreator = (response: UserProfileDetailsResponse): UserProfileDetailsResponse =>
  response;
