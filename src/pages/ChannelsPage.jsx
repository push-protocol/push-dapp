import { Channels } from 'modules/channels';
import { ContentLayout } from 'common';

const ChannelsPage = () => {
  return (
    <ContentLayout>
      <Channels />
    </ContentLayout>
  );
};
export default ChannelsPage;
